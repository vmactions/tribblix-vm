          if [ -n "test" ]; then
            echo "false"
          fi
          if [ "test" ]; then
            echo "test"
          fi
          pwd
          ls -lah
          pwd
          env
          uname -a
          cat /etc/release
          echo "OK"